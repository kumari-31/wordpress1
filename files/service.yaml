apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
  labels:
    app: wordpress
spec:
  type: Nodeport
  selector:
    app: wordpress
  ports:
    - name: http
      port: 80
      targetPort: 9000
      nodeport: 31300

